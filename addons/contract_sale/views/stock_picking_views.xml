<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Stock Picking Form View Extension -->
    <record id="view_picking_form_inherit_contract" model="ir.ui.view">
        <field name="name">stock.picking.form.contract</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_sale_order" type="object" icon="fa-shopping-cart"
                        class="oe_stat_button" invisible="not sale_order_id">
                    <span class="o_stat_text">Đơn bán hàng</span>
                </button>
                <button name="action_view_contract" type="object" icon="fa-file-text-o"
                        class="oe_stat_button" invisible="not sale_order_id">
                    <span class="o_stat_text">Hợp đồng</span>
                </button>
            </xpath>
            <xpath expr="//field[@name='origin']" position="after">
                <field name="sale_order_id" readonly="1"/>
                <field name="purchase_order_id" readonly="1"/>
            </xpath>
        </field>
    </record>

    <!-- Stock Picking List View Extension -->
    <record id="vpicklist_inherit_contract" model="ir.ui.view">
        <field name="name">stock.picking.list.contract</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.vpicktree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='origin']" position="after">
                <field name="sale_order_id" optional="show"/>
            </xpath>
        </field>
    </record>
</odoo>
