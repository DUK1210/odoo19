<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Purchase Order Form View Extension -->
    <record id="purchase_order_form_inherit_contract" model="ir.ui.view">
        <field name="name">purchase.order.form.contract</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_sale_order" type="object" icon="fa-shopping-cart"
                        class="oe_stat_button" invisible="not sale_order_id">
                    <span class="o_stat_text">Đơn bán hàng</span>
                </button>
            </xpath>
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="sale_order_id" options="{'no_create': True}"/>
            </xpath>
            <xpath expr="//field[@name='order_line']/list/field[@name='product_qty']" position="after">
                <field name="sale_order_line_id" invisible="1"/>
            </xpath>
        </field>
    </record>

    <!-- Purchase Order List View Extension -->
    <record id="purchase_order_list_inherit_contract" model="ir.ui.view">
        <field name="name">purchase.order.list.contract</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="sale_order_id" optional="show"/>
            </xpath>
        </field>
    </record>
</odoo>
